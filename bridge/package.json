{
  "name": "@bookmarkops/mcp",
  "version": "0.1.3",
  "description": "MCP server — lets any AI agent manage your Chrome bookmarks via BookmarkOps",
  "type": "module",
  "bin": {
    "bookmarkops-mcp": "src/index.js",
    "mcp": "src/index.js"
  },
  "files": [
    "src/"
  ],
  "scripts": {
    "build": "esbuild src/index.js --bundle --platform=node --format=esm --outfile=dist/bridge.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "engines": {
    "node": ">=18"
  }
}
