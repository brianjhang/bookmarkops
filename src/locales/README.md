# Locales

Chrome extension manifest i18n lives in root `_locales/`.

This folder is reserved for app-level locale helpers if the dashboard needs runtime language switching beyond Chrome manifest messages.
